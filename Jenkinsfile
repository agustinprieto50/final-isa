pipeline {
    agent any

    stages {
        stage('Test Docker') {
            steps {
                script {
                    sh 'sudo docker --version'
                }
            }
        }
    }
}
